# 603d — Cross-Repository Impact Analysis
**Role:** backend **Priority:** 2 **Scope:** large

Service dependency graph. When task modifies shared interface → auto-spawn verification in downstream repos. Block merge until compat confirmed. Enterprise microservices.
