import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Agents() {
  return (
    <PlaceholderScreen
      name="Agents"
      ticket="2026-05-15-frontend-agents-screen.md"
      description="Status grid of currently-running agents with role icon, duration counter, tokens used, and current task. Drill-in opens a drawer with live log tail and side-by-side compare."
    />
  );
}
