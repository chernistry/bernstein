# 511d — CLI Handoff: Other Agents Delegate to Bernstein
**Role:** backend **Priority:** 1 **Scope:** medium

`bernstein delegate --task 'fix flaky auth tests'` — any CLI agent can delegate a subtask. Returns task ID for polling. Makes Bernstein composable inside other agent workflows. Positions as infrastructure, not competitor.
