import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Tasks() {
  return (
    <PlaceholderScreen
      name="Tasks"
      ticket="2026-05-15-frontend-tasks-screen.md"
      description="Live data table of orchestration tasks with search syntax (agent:claude status:running), status filters, and click-through to a tabbed detail view (Log / Diff / Tokens / Cost / Trace)."
    />
  );
}
