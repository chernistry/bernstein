import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Approvals() {
  return (
    <PlaceholderScreen
      name="Approvals"
      ticket="2026-05-15-frontend-approvals-screen.md"
      description="Pending tool-call approval queue with full args diff, risk score, and one-click approve / deny / always-allow / always-deny actions. Live notifications via SSE."
    />
  );
}
